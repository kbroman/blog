---
title: "ask me about atan2"
author: Karl
date: '2026-05-14'
slug: atan2
categories: [r, programming]
tags: [minecraft, r, programming]
---

The semester is done, so I've turned to something light and fun:
revisiting the packages [miner](https://github.com/kbroman/miner) and
[craft](https://github.com/kbroman/craft) for
[R](https://www.r-project.org) programming with
[Minecraft](https://minecraft.net), created at the [2017 ROpenSci
UnConf](https://unconf17.ropensci.org/). (See my past posts on the
topic: [ROpenSci Unconf
2017](https://kbroman.org/blog/2017/05/27/ropensci-unconf-2017/),
[miner and
craft](https://kbroman.org/blog/2017/05/31/miner-and-craft/), and
[writing text in Minecraft with
R](https://kbroman.org/blog/2019/02/11/writing_text_in_minecraft_with_r/).
Also see our ebook, [Programming R with
Minecraft](https://kbroman.org/miner_book).)

I first wanted to see if it all still worked, particularly as the
[RaspberryJuice plugin](https://github.com/zhuowei/RaspberryJuice)
which we rely on is
no longer under development. So that meant firing up a minecraft
server on my laptop, and on a [Raspberry Pi](https://raspberrypi.com),
and in a [Docker](https://www.docker.com) container. (Ugh, installing
docker was a pain in the ass. In the end I got it to work, but I'm not
sure how. Finding and installing the right version of Java was not so
fun either.)

Anyway, I was working on incorporating a [bunch of code that Felix Ling had
sent me years ago](https://github.com/kbroman/craft/issues/5). In
particular,
I was puzzling over his function to build a set of stairs, which would
sometimes put them behind me instead of (as intended) in front of
me.

It turns out our
[`miner::getPlayerRotation()`](https://github.com/kbroman/miner/blob/master/R/getPlayerOrientation.R#L26-L42)
function gave different results
depending on whether `player_id` was specified, but only for some
orientations. Underneath the hood, we call the API function
`entity.getRotation()` if `player_id` is specified but
`player.getRotation()` if it's not, and the latter was giving

So I used
[`miner::setPlayerRotation()`](https://github.com/kbroman/miner/blob/master/R/setPlayerOrientation.R#L25-L30)
in a loop over angles (making my Minecraft player spin around in a
circle) and polled the `getPlayerRotation()` function with and without
specifying `player_id`, and also
[`miner::getPlayerDirection()`](https://github.com/kbroman/miner/blob/master/R/getPlayerOrientation.R#L111-L122)
which returns a unit vector to indicate direction, rather than an
angle. (Note that there's a related function
[`miner::getPlayerPitch()`](https://github.com/kbroman/miner/blob/master/R/getPlayerOrientation.R#L70-L81)
for determining the up/down angle of where you're facing.)

I can't tell you how long I spent puzzling over the conversion between
Cartesian and polar coordinates. Isn't this easy?
`$x=r sin(\theta)$` and `$y = r cos(\theta)$`, and then to go back you
have `$r = \sqrt{x^2 + y^2}$` and `$\theta = atan(y/x)$`, right?.

But WTF `$atan(y/x)$` only returns a value in `$(-\pi/2, \pi/2)$`, not the whole
circle.

Finally, I turned to [the wikipedia page on polar
coordinates](https://en.wikipedia.org/wiki/Polar_coordinate_system)
and learned about `$atan2(y,x)$`, which I'm sure I'd never heard of
before.

So it's not really been what I'd call "light", but it's definitely been
fun. And I'm learning about docker (though it likely won't seem like I did
the next time I have to revisit it), and math, and [how to get fences
to connect
up](https://github.com/kbroman/craft/commit/cc8dbef496aa4fbf6d2eba68394f842000aed91d),
and I'm making a mess of my Minecraft world.

[![Minecraft world after messing with the miner package](/images/2026-05-14_minecraft_small.png)](/images/2026-05-14_minecraft.png)
